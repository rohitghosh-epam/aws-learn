Remove-Item -Path "$env:USERPROFILE\.aws\config" -Force 
Remove-Item -Path "$env:USERPROFILE\.aws\credentials" -Force



give the cli syndicate commands for the same and do create json where ever required. tell me what to do step by step from aws configure. give all the commands in one line